# Claude Notes

Follow the shared project guidance in `AGENTS.md`.

Most importantly: keep this static consultancy website professional from a software standpoint. Preserve the separated `index.html`, `styles.css`, and `contact.js` structure; keep crawler files and README documentation in sync; and avoid changes that make the source look like an unmaintained brochure page.
